{
  "name": "aro-website",
  "version": "2.0.0",
  "description": "African Reclaimers Organisation — Firebase + Vercel",
  "scripts": {
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}